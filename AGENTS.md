# AGENTS.md

## GitNexus 使用约定

本仓库包含 GitNexus 索引（`.gitnexus/` 目录），机器上装有全局 `gitnexus` CLI。除非用户通过 `/gitnexus` 斜杠命令显式要求，否则：

- 不要主动运行任何 `gitnexus` 子命令；
- 不要读取或引用 `.gitnexus/` 目录的内容；
- 了解仓库结构时，使用常规的文件搜索和阅读方式即可。
